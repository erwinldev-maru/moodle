packages:
  yum:
    amazon-efs-utils: []

commands:
  01_mount:
    command: |
      mkdir -p /mnt/moodledata
      mount -t efs -o tls fs-03993581427e418d6:/ /mnt/moodledata
      chown -R webapp:webapp /mnt/moodledata
    test: "! mount | grep /mnt/moodledata"
